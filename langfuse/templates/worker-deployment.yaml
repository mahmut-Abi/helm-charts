{{- if .Values.worker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "langfuse.fullname" . }}-worker
  labels:
    {{- include "langfuse.workerSelectorLabels" . | nindent 4 }}
    {{- include "langfuse.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.worker.replicas }}
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
      {{- include "langfuse.workerSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "langfuse.workerSelectorLabels" . | nindent 8 }}
        {{- with .Values.worker.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.worker.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "langfuse.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.clickhouse.url }}
      {{- $chHost := .Values.clickhouse.url | splitList "://" | last | splitList ":" | first }}
      initContainers:
        - name: init-clickhouse-db
          image: {{ printf "%s:%s" .Values.image.initContainer.repository .Values.image.initContainer.tag }}
          imagePullPolicy: {{ .Values.image.initContainer.pullPolicy }}
          securityContext:
            {{- toYaml .Values.initContainer.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.initContainer.resources | nindent 12 }}
          command:
            - sh
            - -c
            - |
              until curl -sS -u "${CH_USER}:${CH_PASSWORD}" "${CH_SCHEME}://${CH_HOST}:${CH_PORT}/" --data-binary "CREATE DATABASE IF NOT EXISTS ${CH_DB}"; do
                echo "Waiting for ClickHouse..."
                sleep 2
              done
              echo "ClickHouse database '${CH_DB}' ready."
          env:
            - name: CH_HOST
              value: {{ $chHost | quote }}
            - name: CH_PORT
              value: {{ .Values.clickhouse.httpPort | quote }}
            - name: CH_SCHEME
              value: {{ ternary "https" "http" .Values.clickhouse.tlsEnabled | quote }}
            - name: CH_USER
              value: {{ .Values.clickhouse.user | quote }}
            - name: CH_DB
              value: {{ .Values.clickhouse.database | quote }}
            - name: CH_PASSWORD
              {{- include "langfuse.clickhousePasswordEnvValue" . | nindent 14 }}
      {{- end }}
      containers:
        - name: langfuse-worker
          image: {{ include "langfuse.workerImage" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.worker.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: SALT
              valueFrom:
                secretKeyRef:
                  name: {{ include "langfuse.secretName" . }}
                  key: SALT
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "langfuse.secretName" . }}
                  key: ENCRYPTION_KEY
            - name: NEXTAUTH_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "langfuse.secretName" . }}
                  key: NEXTAUTH_SECRET
            - name: NEXTAUTH_URL
              value: {{ .Values.auth.nextauthUrl | quote }}
            - name: DATABASE_URL
              {{- include "langfuse.databaseUrlEnvValue" . | nindent 14 }}
            {{- if .Values.worker.nodeOptions }}
            - name: NODE_OPTIONS
              value: {{ .Values.worker.nodeOptions | quote }}
            {{- end }}
            {{- if .Values.clickhouse.url }}
            - name: CLICKHOUSE_URL
              value: {{ .Values.clickhouse.url | quote }}
            {{- end }}
            {{- if or .Values.clickhouse.migrationUrl .Values.clickhouse.url }}
            - name: CLICKHOUSE_MIGRATION_URL
              {{- include "langfuse.clickhouseMigrationUrlEnvValue" . | nindent 14 }}
            {{- end }}
            - name: CLICKHOUSE_USER
              value: {{ .Values.clickhouse.user | quote }}
            - name: CLICKHOUSE_PASSWORD
              {{- include "langfuse.clickhousePasswordEnvValue" . | nindent 14 }}
            - name: CLICKHOUSE_DB
              value: {{ .Values.clickhouse.database | quote }}
            - name: CLICKHOUSE_CLUSTER_ENABLED
              value: {{ .Values.clickhouse.clusterEnabled | quote }}
            - name: CLICKHOUSE_CLUSTER_NAME
              value: {{ default "default" .Values.clickhouse.clusterName | quote }}
            - name: CLICKHOUSE_MIGRATION_SSL
              value: {{ .Values.clickhouse.tlsEnabled | quote }}
            - name: LANGFUSE_AUTO_CLICKHOUSE_MIGRATION_DISABLED
              value: {{ .Values.clickhouse.autoMigrationDisabled | quote }}
            {{- if or .Values.redis.connectionString .Values.redis.existingSecret }}
            - name: REDIS_CONNECTION_STRING
              {{- include "langfuse.redisConnectionStringEnvValue" . | nindent 14 }}
            {{- end }}
            {{- if .Values.redis.host }}
            - name: REDIS_HOST
              value: {{ .Values.redis.host | quote }}
            {{- end }}
            {{- if .Values.redis.port }}
            - name: REDIS_PORT
              value: {{ .Values.redis.port | quote }}
            {{- end }}
            {{- if .Values.redis.tlsEnabled }}
            - name: REDIS_TLS_ENABLED
              value: "true"
            {{- end }}
            - name: LANGFUSE_S3_EVENT_UPLOAD_BUCKET
              value: {{ .Values.s3.bucket | quote }}
            - name: LANGFUSE_S3_EVENT_UPLOAD_ENDPOINT
              value: {{ .Values.s3.endpoint | quote }}
            - name: LANGFUSE_S3_EVENT_UPLOAD_REGION
              value: {{ .Values.s3.region | quote }}
            - name: LANGFUSE_S3_EVENT_UPLOAD_ACCESS_KEY_ID
              {{- include "langfuse.s3AccessKeyIdEnvValue" . | nindent 14 }}
            - name: LANGFUSE_S3_EVENT_UPLOAD_SECRET_ACCESS_KEY
              {{- include "langfuse.s3SecretAccessKeyEnvValue" . | nindent 14 }}
            - name: LANGFUSE_S3_EVENT_UPLOAD_FORCE_PATH_STYLE
              value: {{ .Values.s3.forcePathStyle | toString | quote }}
            {{- with .Values.worker.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.worker.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
          ports:
            - containerPort: 3030
              name: http
              protocol: TCP
          {{- if .Values.worker.startupProbe.enabled }}
          startupProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.worker.startupProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.worker.startupProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.worker.startupProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.worker.startupProbe.failureThreshold }}
          {{- end }}
          {{- if .Values.worker.readinessProbe.enabled }}
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.worker.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.worker.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.worker.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.worker.readinessProbe.failureThreshold }}
          {{- end }}
          {{- if .Values.worker.livenessProbe.enabled }}
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: {{ .Values.worker.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.worker.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.worker.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.worker.livenessProbe.failureThreshold }}
          {{- end }}
      {{- with .Values.worker.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.worker.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      {{- with .Values.worker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.worker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.worker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.worker.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
