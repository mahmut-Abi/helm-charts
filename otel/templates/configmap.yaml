apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "otel.fullname" . }}-config
  labels:
    {{- include "otel.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}