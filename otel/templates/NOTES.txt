OpenTelemetry Collector has been deployed.

Release name:
  {{ .Release.Name }}

OTLP gRPC endpoint:
  {{ include "otel.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.otlpGrpcPort }}

OTLP HTTP endpoint:
  {{ include "otel.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.otlpHttpPort }}

Prometheus exporter:
  {{ include "otel.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.prometheusExportPort }}

Check collector health:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "otel.fullname" . }} {{ .Values.service.healthCheckPort }}:{{ .Values.service.healthCheckPort }}
  curl http://localhost:{{ .Values.service.healthCheckPort }}

{{- $otlpExporter := get .Values.config.exporters "otlp" | default dict }}
{{- if not (get $otlpExporter "endpoint") }}
External OTLP export is not configured. Add config.exporters.otlp and include
otlp in the pipelines when you want to forward telemetry to another backend.
{{- end }}
