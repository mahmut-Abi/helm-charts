{{- if .Values.snuba.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentry.fullname" . }}-snuba-api
  labels:
    {{- include "sentry.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.snuba.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentry.snubaApiSelectorLabels" . | nindent 4 }}
{{- end }}
